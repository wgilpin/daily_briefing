# daily_briefing Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-30

## Active Technologies
- Python 3.13+ + Flask, HTMX, google-api-python-client, google-auth libraries, markdown conversion library (html2text or similar), LLM client library (OpenAI API or similar) (002-newsletter-aggregator)
- Local file system (JSON files for configuration, SQLite for processed email tracking, file system for emails/markdown/parsed data) (002-newsletter-aggregator)

- Python 3.13+ (project already requires >=3.13) + pyzotero (Zotero API client), python-dotenv (credential management), tqdm (progress bars), argparse (stdlib CLI) (001-zotero-api-digest)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.13+ (project already requires >=3.13): Follow standard conventions

## Recent Changes
- 002-newsletter-aggregator: Added Python 3.13+ + Flask, HTMX, google-api-python-client, google-auth libraries, markdown conversion library (html2text or similar), LLM client library (OpenAI API or similar)
- 002-newsletter-aggregator: Added Python 3.13+ + Flask, HTMX, google-api-python-client, google-auth libraries, markdown conversion library (html2text or similar), LLM client library (OpenAI API or similar)

- 001-zotero-api-digest: Added Python 3.13+ (project already requires >=3.13) + pyzotero (Zotero API client), python-dotenv (credential management), tqdm (progress bars), argparse (stdlib CLI)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
