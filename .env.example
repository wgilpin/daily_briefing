# Daily Briefing - Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# =====================================
# Database (required for Coolify deployment)
# =====================================
# PostgreSQL connection string from Coolify
DATABASE_URL=postgresql://user:password@localhost:5432/daily_briefing

# =====================================
# Zotero Configuration (required)
# =====================================
# Get your credentials from: https://www.zotero.org/settings/keys
# 1. Note your User ID (shown as "Your userID for use in API calls")
# 2. Create a new private key
# 3. Copy the API key (you'll only see it once)
ZOTERO_LIBRARY_ID=your_user_id_here
ZOTERO_API_KEY=your_api_key_here

# =====================================
# Google Gemini API (required for newsletter parsing)
# =====================================
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# =====================================
# OAuth Token Encryption (required for Gmail OAuth)
# =====================================
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
ENCRYPTION_KEY=your_32_byte_hex_key

# =====================================
# Google OAuth (optional, for Gmail integration and user login)
# =====================================
# Create OAuth credentials in Google Cloud Console
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_oauth_client_id
GOOGLE_CLIENT_SECRET=your_oauth_client_secret
GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback

# =====================================
# Flask Session (required for user authentication)
# =====================================
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_flask_secret_key

# IMPORTANT: Public registration is DISABLED for security
# This is a personal app - your feed data is shared across all users
# To create user accounts, use: python create_user.py

# =====================================
# Email/SMTP (required for password reset)
# =====================================
# Configure your SMTP server for sending password reset emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_smtp_password_or_app_password
FROM_EMAIL=noreply@yourdomain.com

# =====================================
# ElevenLabs Text-to-Speech (optional for audio newsletters)
# =====================================
# Get your API key from: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your_api_key_here
# Optional voice IDs (defaults provided below)
ELEVENLABS_MALE_VOICE_ID=ErXwobaYiN019PkySvjV
ELEVENLABS_FEMALE_VOICE_ID=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_MODEL=eleven_monolingual_v1
ELEVENLABS_TIMEOUT=30
